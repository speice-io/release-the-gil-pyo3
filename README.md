[Maturin](https://pypi.org/project/maturin/) is used to manage this project.

To set it up in development mode, run `maturin develop`. To compile the Rust portion in release
mode, run `maturin develop --release`.
